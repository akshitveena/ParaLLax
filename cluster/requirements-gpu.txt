# Box-side dependencies only. Deliberately NOT the root requirements.txt:
#   - torch is handled by setup_venv.sh (it reuses a driver-matched system torch when one
#     exists, because installing a fresh wheel over a mismatched driver is the #1 way a GPU
#     box silently falls back to CPU or fails at the first .cuda() call).
#   - anthropic / groq / ollama are omitted on purpose: the only API experiment (2d) runs on
#     your Mac where .env lives. No API key should ever be copied to a rented/shared box.
transformers>=4.40.0
sentence-transformers>=3.0.0
accelerate>=0.30.0        # device_map="auto" for the 7B PRM in 2c
safetensors>=0.4.0
scikit-learn>=1.3.0
numpy>=1.24.0
matplotlib>=3.7.0
tqdm>=4.65.0
datasets>=2.0.0
ipykernel>=6.29.0         # so the venv shows up as a JupyterLab kernel
